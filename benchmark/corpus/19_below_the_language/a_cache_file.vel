// Formats a report through a vendored library; the library also writes a cache file of its own.
fn main() uses io, ffi {
    check py("fmtlib", "render", ["report"]) {
        ok out { print(out) }
        fail why { print("no render: " + why) }
    }
}
