name: ci
on: pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo lint
