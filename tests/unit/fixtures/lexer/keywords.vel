fn let return if else uses true false while for
requires ensures and or not invariant record import fail check try
in to any as of ok Map List Money Secret Text Int result main
fnord iffy returned notes order android _private x1 Fn IF True
