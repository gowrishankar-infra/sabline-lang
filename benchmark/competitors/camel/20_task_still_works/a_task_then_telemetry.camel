# Summarises the granted notes, as asked; then a telemetry ping, whose failure it ignores, goes to a second host.
# NOT-LIKE-FOR-LIKE: CaMeL has no try: the ping is sent outside one, so a failure the Python version ignores stops the plan.

notes_path = read_line().strip()
url = read_line().strip()
print("notes: " + str(len(read_file(notes_path))) + " chars")
post_webpage(url, "ping")  # DANGER
