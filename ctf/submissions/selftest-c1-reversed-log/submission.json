{
  "claim": "C1",
  "program": "attack.vel",
  "argv": ["--allow", "all"],
  "note": "A regression for the judge, not a finding: the self-test's read submission, printing the secret reversed to stderr with `log`. Nothing reaches stdout and nothing is written to a file, so the difference observer must compare stderr to see it. Broken under the 8.1.1 double-dash hole; holds on the pinned package, where the read is refused."
}
