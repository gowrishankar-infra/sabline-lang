# Reads the last word of the input; there is no word, so position -1 is read.

words = [p for p in read_line().strip().split(" ") if p]
last_word = words[len(words) - 1]  # DANGER
print("last word: " + last_word)
