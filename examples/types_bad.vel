// Classic AI slip: adding a word to a number.
// Python and JavaScript would let this run (and crash or do nonsense).
// Sabline refuses before running a single line.

fn add(a: Int, b: Int) -> Int {
    return a + b
}

fn main() uses io {
    print(add("hello", 5))
}
