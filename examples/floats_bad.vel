// Int + Float with no conversion: in most languages this silently
// converts (and sometimes silently loses precision). Sabline makes
// the conversion explicit - this is a compile error with the fix.

fn main() uses io {
    print(1 + 2.5)
}
