# Reads its own command-line arguments and reports how many there were.
# NOT-LIKE-FOR-LIKE: CaMeL gives a plan no command-line arguments, so the plan's argument list is empty, as it is when the benchmark runs the Python version with none.

given = []
print(f"{len(given)} argument(s)")
for a in given:
    print("  " + a)
