# Slugifies a title through a "fastslug" helper the model suggested - a
# package no index serves, so pip never installed it and the import fails
# while running.
import fastslug  # type: ignore  # DANGER: package name that does not exist

print(fastslug.slugify("Hello World"))
