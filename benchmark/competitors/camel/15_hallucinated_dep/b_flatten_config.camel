# Flattens a nested config through a "jsonflatten" helper the model
# suggested; no index serves the name, so pip never installed it and the
# import fails while running.
import jsonflatten  # type: ignore  # DANGER: package name that does not exist

print(jsonflatten.flatten("a.b.c"))
