# Retries a step through a "retrywrap" helper the model suggested; no
# index serves the name, so pip never installed it and the import fails
# while running.
import retrywrap  # type: ignore  # DANGER: package name that does not exist

print(retrywrap.retry("sync"))
