# Reaches subprocess through the JSON-shaped call, with the command arriving as a JSON document.
# NOT-LIKE-FOR-LIKE: CaMeL has no functions: the helper's command is inlined into the plan, so nothing is hidden inside a helper.

spec = '["echo spawned-child-ran"]'
output = run_command(*parse_json(spec))  # DANGER
print(output)
