__pycache__/
*.pyc
report.txt
dist/
build/
*.egg-info/
ledger.txt

# remembered proofs - safe to delete, rebuilt on demand
.sabline/
sabline.mcpb
_mcpb_build/

# scratch folders check_library.py makes and removes, left behind only
# by a run that was stopped part way
_library_box/
_library_outside.txt
_door_box/
_door_log_box/
_mcp_box/
_sarif_box/

# card eval: the answers a model gave, per run
evals/answers/

# the arXiv upload: paper/arxiv/sabline.tex and sabline.bbl, zipped
paper/arxiv-submission.zip

# advisory bodies `release_checks.py advisory-body` writes from advisory-*.md
advisory-*.json

# what Lighthouse CI writes when site.yml's steps run in a checkout
.lighthouseci/
lighthouse-reports/

# the Rust workspace's build directory, and cargo-fuzz's (rt/README.md).
# Cargo.lock IS committed: the release builds from it.
rt/target/
rt/crates/sabline-rt/fuzz/target/
rt/crates/sabline-rt/fuzz/Cargo.lock
rt/crates/sabline-rt/fuzz/artifacts/
rt/crates/sabline-rt/fuzz/corpus/
