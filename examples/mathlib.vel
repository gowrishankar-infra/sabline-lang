// A tiny library: no main(), meant to be imported by other files.
// Its promises are proven like any other Sabline code.

fn square(n: Int) -> Int
    ensures result >= 0
{
    return n * n
}

fn cube(n: Int) -> Int {
    return n * n * n
}
